name: Rust

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  tucant-language-server:
    uses: ./.github/workflows/rust.yml
    with:
      path: tucant-language-server
