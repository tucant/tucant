default:
  tags:
    - nix

build-job:
  stage: build
  script:
    - nix --extra-experimental-features nix-command --extra-experimental-features flakes build -L .#checks.x86_64-linux.my-app-fmt .#extension
  artifacts:
    expose_as: tucant-extension
    name: tucant-extension
    paths:
      - result